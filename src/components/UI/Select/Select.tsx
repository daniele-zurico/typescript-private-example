import * as React from 'react';

interface IProps {
    data: string[];
}

const select = (props: IProps) => {
    return (
        <div>
            hello
        </div>
    )
}

export default select;
